# Fixture: format-changed (current)
# Demonstrates: format changed; expect: no breaking (property-format-changed warning)
# Generated by scripts/gen-fixtures.ts — do not edit by hand.
openapi: 3.1.0
info:
  title: Petstore API
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      summary: List users
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserList'
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    User:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
        email:
          type: string
          format: uri

    UserInput:
      type: object
      properties:
        name:
          type: string
        bio:
          type: string
        handle:
          type: string

    UserList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/User'

    Error:
      type: object
      properties:
        message:
          type: string
