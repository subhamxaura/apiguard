# Fixture: yaml-reorder-quote-changes-only (current)
# Demonstrates: YAML reorder + quote changes only; expect: zero changes (semantic identity)
# Generated by scripts/gen-fixtures.ts — do not edit by hand.
openapi: '3.0.3'
info:
  version: 1.0.0
  title: 'Petstore API'
paths:
  /users:
    get:
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserList'
      operationId: listUsers
      summary: List users
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
components:
  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
    UserList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/User'
    User:
      required:
        - id
        - name
      type: object
      properties:
        email:
          type: string
        name:
          type: string
        id:
          type: string
